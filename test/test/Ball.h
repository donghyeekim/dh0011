#pragma once
#include <iostream>

class Ball
{
	public:
		Ball();
		~Ball();
		CueBall(double diameter, int x, int y);
		First_ObjectBall(double diameter, int x, int y);
		Second_ObjectBall(double diameter, int x, int y);
	private:

		
};

